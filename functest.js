people = [{'name': 'Mary', 'height': 160},
          {'name': 'Isla', 'height': 80},
          {'name': 'Sam'}];
var heights = people.filter(function (person) {
	return person.height;
}).map(function(person) {
	return person.height;
});

function add (a,b) {
	return a + b;
}







var avgHeight = heights.reduce(
console.log(heights);
